보안 키패드 앱 프로젝트 설계서
1. 기술 스택 추천
Compose 추천 (강력 권장)
이유:

커스텀 UI가 핵심: 키패드는 커스텀 뷰 위주인데 Compose가 훨씬 간결하고 유연함
테마/색상 변경: Compose의 MaterialTheme로 즉시 대응 가능
리팩토링: State 관리가 명확하고 재사용성 높음
학습 가치: 2025년 현재 Compose는 필수 스킬, 이번 기회에 익히면 일석이조
개발 속도: 초기 러닝커브 있지만 결과적으로 더 빠름 (XML은 보일러플레이트 많음)

주말 2시간으로 충분히 가능한 이유:

키패드 UI는 단순 그리드 구조
Compose 기본만 알면 됨 (Column, Row, Button, remember)


2. 프로젝트 범위 정의 (MVP)
2.1 필수 기능 (Phase 1)
✅ 숫자 키패드 (0-9, 백스페이스)
✅ 기본 테마 (라이트/다크)
✅ 입력 마스킹 (●●● 표시)
✅ 샘플 액티비티에 적용
2.2 확장 기능 (Phase 2)
✅ 영문 키패드 (QWERTY)
✅ 한글 키패드 (천지인 or 쿼티)
✅ 커서 이동
✅ 색상 커스터마이징

3. 아키텍처 설계
3.1 모듈 구조
secure-keypad/
├── app/                          # 샘플 앱
└── keypad/                       # 키패드 라이브러리 (재사용 가능)
    ├── ui/
    │   ├── SecureKeypad.kt      # 메인 Composable
    │   ├── KeypadButton.kt      # 개별 키 버튼
    │   ├── KeypadLayout.kt      # 레이아웃 정의
    │   └── theme/
    │       └── KeypadTheme.kt   # 테마 설정
    ├── data/
    │   ├── KeypadType.kt        # Enum: NUMERIC, ENGLISH, KOREAN
    │   ├── KeypadLayout.kt      # 레이아웃 데이터 클래스
    │   └── SecureBuffer.kt      # 암호화 버퍼
    └── viewmodel/
        └── KeypadViewModel.kt   # 입력 로직
3.2 핵심 클래스 설계
kotlin// 1. 키패드 타입
enum class KeypadType {
    NUMERIC,      // 숫자
    ENGLISH,      // 영문
    KOREAN        // 한글
}

// 2. 키 데이터
data class Key(
    val value: String,
    val displayText: String,
    val type: KeyType = KeyType.NORMAL
)

enum class KeyType {
    NORMAL,       // 일반 키
    BACKSPACE,    // 삭제
    SPACE,        // 스페이스
    SWITCH        // 언어 전환
}

// 3. 테마
data class KeypadTheme(
    val backgroundColor: Color,
    val keyColor: Color,
    val keyTextColor: Color,
    val specialKeyColor: Color
)

// 4. 보안 버퍼 (입력값 암호화)
class SecureBuffer {
    private val encrypted = mutableListOf<ByteArray>()
    
    fun add(char: Char)
    fun removeLast()
    fun getDecrypted(): String
    fun clear()
}

4. 기능 명세서
4.1 숫자 키패드
[1] [2] [3]
[4] [5] [6]
[7] [8] [9]
[⌫] [0] [✓]

3x4 그리드
백스페이스, 확인 버튼
입력시 마스킹 (●)

4.2 영문 키패드
[Q] [W] [E] [R] [T] [Y] [U] [I] [O] [P]
[A] [S] [D] [F] [G] [H] [J] [K] [L]
[⇧] [Z] [X] [C] [V] [B] [N] [M] [⌫]
[한/영] [Space] [✓]
4.3 한글 키패드 (천지인 간소화)
[ㅂ] [ㅈ] [ㄷ] [ㄱ] [ㅅ] [ㅛ] [ㅕ] [ㅑ] [ㅐ] [ㅔ]
[ㅁ] [ㄴ] [ㅇ] [ㄹ] [ㅎ] [ㅗ] [ㅓ] [ㅏ] [ㅣ]
[⇧] [ㅋ] [ㅌ] [ㅊ] [ㅍ] [ㅠ] [ㅜ] [ㅡ] [⌫]
[한/영] [Space] [✓]
4.4 보안 기능
kotlin// 입력 이벤트 리스너
interface SecureKeypadListener {
    fun onKeyPressed(maskedValue: String)  // ●●● 형태로 전달
    fun onComplete(secureBuffer: SecureBuffer)  // 완료시 암호화된 버퍼 전달
}
4.5 커스터마이징
kotlinSecureKeypad(
    type = KeypadType.NUMERIC,
    theme = KeypadTheme(
        backgroundColor = Color.White,
        keyColor = Color.LightGray,
        keyTextColor = Color.Black
    ),
    maskedChar = '●',
    listener = listener
)

5. 개발 일정 (8주, 주말 2시간)
Week 1-2: 프로젝트 셋업 & 숫자 키패드 (4시간)

 프로젝트 생성, Gradle 설정
 숫자 키패드 UI (Compose 기본)
 입력 처리 로직
 마스킹 기능

결과물: 숫자만 입력 가능한 기본 키패드

Week 3-4: 테마 & 보안 (4시간)

 테마 시스템 구축 (라이트/다크)
 SecureBuffer 구현 (AES 암호화)
 색상 커스터마이징 API
 샘플 앱에서 테스트

결과물: 테마 전환 가능, 입력값 암호화

Week 5-6: 영문/한글 키패드 (4시간)

 영문 QWERTY 레이아웃
 한글 레이아웃 (간소화 버전)
 키패드 타입 전환 UI
 백스페이스, 스페이스 처리

결과물: 3종 키패드 전환 가능

Week 7: 마무리 & 리팩토링 (2시간)

 코드 정리 및 주석
 README 작성
 샘플 앱 고도화 (비밀번호 입력 화면)

결과물: 배포 가능한 라이브러리

Week 8: 적용 & 문서화 (2시간)

 사내 프로젝트 적용 (로그인 화면 등)
 기술 문서 작성
 KPI 자료 준비

결과물: 실제 적용 사례, KPI 달성

6. 핵심 구현 가이드
6.1 Compose 기본 구조
kotlin@Composable
fun SecureKeypad(
    type: KeypadType = KeypadType.NUMERIC,
    theme: KeypadTheme = KeypadTheme.default(),
    onKeyPressed: (String) -> Unit
) {
    val viewModel: KeypadViewModel = viewModel()
    val inputState by viewModel.maskedInput.collectAsState()
    
    Column(
        modifier = Modifier
            .fillMaxWidth()
            .background(theme.backgroundColor)
    ) {
        // 입력 표시 영역
        InputDisplay(text = inputState)
        
        // 키패드 그리드
        KeypadGrid(
            keys = getLayoutForType(type),
            theme = theme,
            onKeyClick = { key -> 
                viewModel.handleKeyPress(key)
                onKeyPressed(viewModel.getMaskedValue())
            }
        )
    }
}
6.2 상태 관리 (ViewModel)
kotlinclass KeypadViewModel : ViewModel() {
    private val secureBuffer = SecureBuffer()
    private val _maskedInput = MutableStateFlow("")
    val maskedInput: StateFlow<String> = _maskedInput.asStateFlow()
    
    fun handleKeyPress(key: Key) {
        when (key.type) {
            KeyType.NORMAL -> {
                secureBuffer.add(key.value[0])
                updateMaskedDisplay()
            }
            KeyType.BACKSPACE -> {
                secureBuffer.removeLast()
                updateMaskedDisplay()
            }
        }
    }
    
    private fun updateMaskedDisplay() {
        _maskedInput.value = "●".repeat(secureBuffer.length())
    }
}
6.3 보안 버퍼 (간단 버전)
kotlinclass SecureBuffer {
    private val data = mutableListOf<Char>()
    
    fun add(char: Char) {
        data.add(char)
    }
    
    fun removeLast() {
        if (data.isNotEmpty()) data.removeLast()
    }
    
    fun getDecrypted(): String {
        // 실제로는 AES 암호화/복호화
        return String(data.toCharArray())
    }
    
    fun length() = data.size
    
    fun clear() {
        data.clear()
    }
}

7. 성공 기준 (KPI)
✅ 기능 완성도

 3종 키패드 동작 (숫자/영문/한글)
 테마 전환 (2종 이상)
 입력 마스킹

✅ 코드 품질

 MVVM 패턴 적용
 재사용 가능한 라이브러리 구조
 주석 및 문서화

✅ 실제 적용

 샘플 앱 구현
 사내 프로젝트 1곳 적용 (로그인/결제 등)


8. 추천 학습 리소스
Compose 빠른 시작 (1시간이면 충분)
1. Compose 기본: Column, Row, Box, Button
2. State 관리: remember, mutableStateOf
3. ViewModel 연동: collectAsState()
추천 튜토리얼:

Android Developers: Jetpack Compose 기초 (공식 문서)
"Compose in 100 seconds" 유튜브 영상들


9. 리스크 & 대응책
리스크대응책Compose 학습 부족Week 1에 튜토리얼 1시간 투자 (주중 저녁)한글 입력 복잡도간소화 레이아웃 사용 (조합 없이 완성형만)시간 부족Phase 1만 완성해도 KPI 달성 가능암호화 구현 어려움초기엔 단순 마스킹만, 나중에 고도화

10. 결론 및 첫 스텝
🎯 이번 주말 할 일 (2시간)

30분: Android Studio 프로젝트 생성, Compose 의존성 추가
60분: 숫자 키패드 UI 구현 (3x4 그리드, 버튼만)
30분: 간단한 입력 처리 및 화면 표시

kotlin// 이 코드만 완성하면 첫 주 성공!
@Composable
fun NumericKeypad() {
    val keys = listOf(
        "1", "2", "3",
        "4", "5", "6",
        "7", "8", "9",
        "⌫", "0", "✓"
    )
    
    LazyVerticalGrid(columns = GridCells.Fixed(3)) {
        items(keys) { key ->
            Button(
                onClick = { /* TODO */ },
                modifier = Modifier.aspectRatio(1f)
            ) {
                Text(key)
            }
        }
    }
}
시작하시면 Week 1 진행 상황 공유해주세요. 막히는 부분 바로 도와드리겠습니다! 💪